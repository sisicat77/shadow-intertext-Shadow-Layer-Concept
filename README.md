# shadow-intertext-Shadow-Layer-Concept
The Shadow Layer is a native parallel module inside the model — it injects guidance vectors into the residual stream only for marked text, without modifying weights, without text prompts, with zero interference.
